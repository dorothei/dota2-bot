meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  scriptName: null
  command: python -m app
  persistenceMount: /data
  containerPort: "80"
  servicePort: "80"
